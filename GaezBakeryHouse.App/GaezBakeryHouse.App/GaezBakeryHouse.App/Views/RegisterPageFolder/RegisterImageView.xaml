<?xml version="1.0" encoding="UTF-8" ?>
<ContentView
    x:Class="GaezBakeryHouse.App.Views.RegisterPageFolder.RegisterImageView"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:fonts="clr-namespace:GaezBakeryHouse.App.Fonts">

    <Grid BackgroundColor="{StaticResource PrimaryColor}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Frame
            Margin="10,10,0,0"
            Padding="5"
            CornerRadius="5"
            HorizontalOptions="Start">
            <Label
                FontSize="35"
                HorizontalOptions="Center"
                Style="{StaticResource LabelIconStyle}"
                Text="{Static fonts:IconFont.ArrowLeft}"
                VerticalOptions="Center" />
        </Frame>
        <Label
            Grid.Row="0"
            Grid.RowSpan="2"
            FontSize="200"
            HorizontalOptions="Center"
            Style="{StaticResource LabelIconStyle}"
            Text="{Static fonts:IconFont.UserOutline}"
            VerticalOptions="Center" />
    </Grid>
</ContentView>