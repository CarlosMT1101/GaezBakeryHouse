<?xml version="1.0" encoding="UTF-8" ?>
<ContentView
    x:Class="GaezBakeryHouse.App.Views.LoginPageFolder.LoginImageView"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:ffimageloading="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms">

    <ContentView.Resources>
        <ResourceDictionary>
            <Style x:Key="imgStyle" TargetType="ffimageloading:CachedImage">
                <Setter Property="HorizontalOptions" Value="Center" />
                <Setter Property="VerticalOptions" Value="CenterAndExpand" />
                <Setter Property="WidthRequest" Value="200" />
                <Setter Property="HeightRequest" Value="200" />
            </Style>
        </ResourceDictionary>
    </ContentView.Resources>

    <Grid BackgroundColor="{StaticResource PrimaryColor}">
        <ffimageloading:CachedImage Source="gaez.png" Style="{StaticResource imgStyle}" />
    </Grid>

</ContentView>